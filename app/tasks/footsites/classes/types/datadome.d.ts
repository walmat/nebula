export type DatadomeData = {
  initialCid: string;
  cid: string;
  hsh: string;
  t: string;
  s: string;
  host: string;
  referer: string;
  site: string;
};

export type DatadomeCookie = {
  cookie: string;
};
